node_modules
/build
/.svelte-kit
/package
.env
.DS_Store
*.local

# Uploaded logos (runtime user content)
/data/uploads

# Machine-local runtime state (do not sync via git)
/data/config.json
/uploads
